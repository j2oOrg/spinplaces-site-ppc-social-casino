apiVersion: apps/v1
kind: Deployment
metadata:
  name: spinplaces-site-ppc-social
  labels:
    app: spinplaces-site-ppc-social
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spinplaces-site-ppc-social
  template:
    metadata:
      labels:
        app: spinplaces-site-ppc-social
    spec:
      containers:
        - name: spinplaces-site-ppc-social
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: spinplaces-site-ppc-social
  labels:
    app: spinplaces-site-ppc-social
spec:
  type: LoadBalancer
  selector:
    app: spinplaces-site-ppc-social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  loadBalancerIP: LOAD_BALANCER_IP_PLACEHOLDER
